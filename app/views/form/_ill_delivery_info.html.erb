<% if book_request? %>
  <%= f.select(
        :deliverytype,
        options_for_select(Illiad::ILL_PICKUP_OPTIONS),
        { label: t('forms.ill.fields.deliverytype'),
          prompt: t('forms.ill.messages.delivery_options_blank') },
        required: true
      )
  %>
<% else %>
  <%# ScanDeliver or Article case %>
  Web Delivery
<% end %>
