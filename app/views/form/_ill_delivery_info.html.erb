<% if book_request %>
  <%= f.select(
        :deliverytype,
        options_for_select(
          [
            ['Van Pelt Library'], ['Books by Mail']
          ]
        ),
        { label: 'Delivery' })
  %>
<% else %>
  <%# ScanDeliver or Article case %>
  Web Delivery
<% end %>
